# Copy to .env and fill in your values
ANTHROPIC_API_KEY=sk-ant-...

# Paths (defaults work if you keep the standard layout)
DB_PATH=data/penny.duckdb
FTS_PATH=data/fts.db
STATEMENTS_DIR=data/statements
